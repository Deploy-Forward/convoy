# Luna briefs (codex gpt-5.6, effort high declared)

Drafted from the thread's provenance and adversarially refuted/repaired (Fable workflow wf_60fc6f01, 2026-09-05). Data contract: docs/CONVOY_SOT.md. Orchestration: docs/briefs/HAPPY_PATH_ULTRACODE.md.


## luna1-happy-path: luna1: provenance rows + rebase check, on the existing .convoy feed

**Provenance observed at drafting:** Read 2026-09-05 from git and the live tape (C:/Users/marco/ola/convoy-wt-happy/.convoy/feed.jsonl, seats.jsonl), not from memory. feat/happy-path-proof: 6fe15c1 on b783950. convoy/g1 (-wt-g1): tip 999b7f9 'Purge ola_runner and move handoffs to .convoy/handoff/.' on efd5e26 'Index hygiene: throwaway test home, threads --prune, recent() for the picker.' (cli.py +10, index.py +88, mcp_http.py +11, thread_index_test.py, tools_delivery_test.py, test/__init__.py, test/demo/__init__.py, .gitignore, README, SECURITY_GATES) on a41a91d (g1's own copy of 6fe15c1; merge-base with base is b783950). g1 committed diff vs base on luna1's files: cli.py +29/-3, mcp_http.py +11/-3, rail.py untouched. g1 UNCOMMITTED now: M README.md, plugin/convoy/skills/convoy/SKILL.md, skills/convoy/SKILL.md, src/convoy/cli.py (+17, the /convoy --start work), ?? src/convoy/start.py, ?? test/demo/start_test.py. g1 feed notes: slice 1 efd5e26 (02:19:35Z), slice 2 ola purge (04:12:57Z). convoy/g2 (-wt-g2): tip 296a8bc 'Allow labelled .ola legacy fallbacks in the handoff grep-gate' on edc7045 (merge of convoy/g1) on 66b7379 'Failure-path tests and grep-gates' on ac816a5 (merge of g1) on 6fe15c1; working tree clean; g2 notes 04:14-04:17Z report grep_gate GREEN on merged g1 999b7f9 and remaining RED items all in g1's slices. convoy/luna1 and convoy/luna2: 6fe15c1, clean. Seats: luna1/luna2 codex gpt-5.6 effort high effort_applied false resume null. luna1 seated row exists (02:19:37Z, token f259aab5...), roll-call notes 02:21:12Z and 02:22:45Z 'no implementation brief received'. luna2 seated 02:20:09Z, 'stopping without code changes' 02:21:36Z. Feed kinds today: conductor 4, join 4, note 17, seated 4, synapse 10, commit 0: no per-chair sha record exists yet. Tool path: `convoy` on PATH = C:\\Users\\marco\\.local\\bin\\convoy.cmd -> C:\\.grok\\Invoke-Convoy.ps1 (builtins help/roster/feed/context/terminals/models/mention/hook/send; hook and send routed to C:\\.grok\\ConvoyLayer.ps1, a second feed writer outside src); `python -c 'import convoy'` fails; `PYTHONPATH=src python -m convoy --help` from -wt-luna1 works and lists the verbs. How the codex chairs' `seated` rows landed through that shim is NOT established; hence slice 0. NOT verified: luna2's brief was not supplied, so overlap with luna2 on provenance/rail/mcp read tools is unchecked; the parent must diff the two briefs before send. docs/briefs/LUNA_BRIEFS.md does not exist on any branch (only HAPPY_PATH_ULTRACODE.md), so send_body carries the brief itself.

**Rebase protocol:** Base is feat/happy-path-proof (tip 6fe15c1 at brief time; convoy/luna1 is at the same sha, clean). Sibling commits are NOT on the base: convoy/g1 = 999b7f9 > efd5e26 > a41a91d > b783950, convoy/g2 = 296a8bc > edc7045 (merge of g1) > 66b7379 > ac816a5 > 6fe15c1. Rule: you rebase onto the BASE only. Never rebase onto, merge, or cherry-pick from a sibling branch; whoever integrates g1/g2/luna1 into feat/happy-path-proof resolves cross-branch overlap, not you. Your job is to make that merge mechanical: keep every cli.py hunk purely additive (new subparsers and new dispatch branches, placed at the end of their blocks, no edits to existing lines), one new TOOLS entry and one new _call_tool branch in mcp_http.py next to `rail`, one new key in rail.build_rail, new files everywhere else. Before starting: `git fetch --all`; `CV feed --since 6h` and read every note from g1-happy-path, g2-happy-path, luna2-happy-path; `git log --oneline feat/happy-path-proof convoy/g1 convoy/g2 convoy/luna2`; `git rebase feat/happy-path-proof`. Before EVERY push: fetch; `git rebase feat/happy-path-proof`; then for each sibling S in convoy/g1 convoy/g2 convoy/luna2 run `git diff --name-only $(git merge-base S HEAD)..S` (after slice 3, `CV rebase --check` does this) and read `CV feed --since 2h`; if a sibling touched cli.py, mcp_http.py, rail.py, CONVOY_SOT.md or a test file you edited, re-read their hunk (`git show S -- <file>`) and re-check that yours stays additive and does not rename or move anything they touched; do not pull their commits. Then python test/run.py GREEN, commit small (never squash, never amend a pushed commit), `CV committed --as-me` (or --as luna1-happy-path), push convoy/luna1, hook note --to grok-bot with sha + files + test count. Files reserved to luna1 by this brief: src/convoy/provenance.py, the three new test files, additive hunks in cli.py/mcp_http.py/rail.py, and the two doc blocks; if luna2's brief names any of these or a provenance/rebase/rail read tool, stop and post a note before writing.


### 0. Prove your tool path and identity before writing code
The `convoy` on this machine's PATH is a legacy PowerShell shim (C:\Users\marco\.local\bin\convoy.cmd -> C:\.grok\Invoke-Convoy.ps1) that knows only help/roster/feed/context/terminals/models/mention/hook/send and routes `hook` to C:\.grok\ConvoyLayer.ps1. It will never see verbs you add. `python -c "import convoy"` fails in a fresh shell (no installed package). The one invocation guaranteed to run YOUR src is, from C:\Users\marco\ola\convoy-wt-happy-wt-luna1: `$env:PYTHONPATH='src'; python -m convoy --root C:\Users\marco\ola\convoy-wt-happy <verb>` (PowerShell) or `PYTHONPATH=src python -m convoy --root ... <verb>` (bash). Call that form CV below; every CV in this brief means exactly that. This is your shell invocation only: g2's grep-gate forbids `-m convoy` and interpreter paths inside src argv literals and resume argv, so never bake `python -m convoy` into code or docs. Run `CV whoami` and `CV hook --as-me --to grok-bot 'luna1 whoami probe'` and post the whoami JSON verbatim. If --as-me is refused from your codex pane (seats.jsonl has resume=null for luna1, and panes.py:266-270 records that a Windows codex pane exposes neither token nor cwd), say so in the note; slice 1 gives you `--as luna1-happy-path` as the fallback. You are already seated (feed row 2026-09-05T02:19:37Z kind=seated, token f259aab5...); do NOT run seated again.

- files: none
- tests: None; this slice is evidence-gathering. The ack note must quote `CV whoami` output and state whether --as-me resolved.
- done when: One hook note --to grok-bot from luna1-happy-path with the whoami JSON and the sentence 'as-me resolves: yes|no'.


### 1. kind=commit feed row, written by the chair after each commit
New module src/convoy/provenance.py with its OWN subprocess wrapper `_git(args, cwd) -> tuple[int|None, str]` returning (returncode, stdout) and (None, '') on OSError/timeout. Do NOT reuse gitstate._run: it returns None both on rc!=0 and on empty stdout (gitstate.py:8-17), which would report an empty-but-successful diff-tree as 'failed'. commit_row(root, instance_id, worktree, rev='HEAD') reads git truth from the worktree: branch (rev-parse --abbrev-ref HEAD), sha (rev-parse <rev>), parent (rev-parse <rev>^ ; null when rc!=0 on a root commit), subject (log -1 --format=%s <rev>), files (diff-tree --no-commit-id --name-only -r <rev>, sorted; [] when rc==0 and stdout empty, e.g. merge or empty commit; null ONLY when rc!=0). It appends ONE row through layer.hook (the single feed writer in src; never a second file): {ts, kind:'commit', instance_id:<chair>, from:<chair>, summary:'commit <sha7> on <branch>: <subject>', branch, sha, parent, files, worktree}. Refuse (ok:false, error) when the worktree is not a git repo or sha is missing: a commit row without a sha is invented, not null. Duplicate protection: same instance_id+sha already on the feed -> {ok:true, duplicate:true}, no second row. CLI: `convoy committed (--as-me | --as <chair>) [--rev HEAD] [--worktree <path>]` as an additive subparser in cli.py: --as-me resolves the chair exactly as `hook --as-me` does (identify(root), refuse when no chair matches); --as <chair> follows the existing precedent (cli.py:123/158/195) and is refused unless <chair> is a session_id in seats.jsonl; --worktree defaults to that chair's seat worktree from seats.jsonl. No token on the row. Dogfood: from this slice on, every luna1 commit is followed by `CV committed --as-me` (or --as luna1-happy-path if slice 0 showed as-me does not resolve).

- files: src/convoy/provenance.py, src/convoy/cli.py, test/demo/provenance_commit_test.py
- tests: Real temp git repo + temp CONVOY_HOME (copy test/demo/happy_path_test.py:49-60): row lands in .convoy/feed.jsonl with kind=commit, from==instance_id==chair, sha == `git rev-parse HEAD`, files == the committed paths sorted, parent null on the first commit; a merge commit (or `git commit --allow-empty`) yields files [] not null; a fake _git returning rc!=0 for diff-tree yields files null and still writes the row; non-git worktree refused, no row written; missing sha refused; --as with a chair not in seats.jsonl refused; --as-me refused with no chair (use panes._TEST_PROCS/_TEST_PID); second call same sha -> duplicate:true and the feed line count is unchanged; the row JSON contains no 'token'.
- done when: python test/run.py GREEN; `CV committed --as-me` (or --as) prints the row; `CV feed --since 1h` shows kind=commit rows for luna1; hook note --to grok-bot citing the row's sha and files.


### 2. Read verb `convoy provenance` and a `provenance` key on the rail
provenance.summarize(root, since=None) folds seats.jsonl + feed kind=commit rows into per-chair state: {chair, harness, worktree, branch, sha, last_commit_ts, commits, files_touched}. `harness` is a straight copy of the seat row's `to` field (docs/CONVOY_SOT.md line 50); `worktree` is the seat row's worktree. Every chair in seats.jsonl appears (latest seat row per session_id wins); a chair with no commit row carries branch/sha/last_commit_ts null and commits 0, files_touched [] — never a guess from the seat's worktree. A commit row whose instance_id has no seat row still appears with harness null. CLI `convoy provenance [--since 1d]` prints {schema_version, since, chairs:[...]}. rail.build_rail gains ONE additive key `provenance: {chairs: N, with_commits: N, tips: [{chair, branch, sha7, ts}]}` from the same fold; do not touch existing rail keys (happy_path_test.py:198-219 asserts them individually). MCP: one read-only tool `provenance` next to `rail` in mcp_http.py TOOLS and one `_call_tool` branch beside `rail`, routed through the same _redact_public path; mcp_wizard_verbs_test derives the public set from TOOLS minus _WRITE_TOOLS, so a read tool passes without editing that test. Readers skip unknown kinds per docs/CONVOY_SOT.md; change no existing row shape.

- files: src/convoy/provenance.py, src/convoy/rail.py, src/convoy/cli.py, src/convoy/mcp_http.py, test/demo/provenance_read_test.py
- tests: Two seated chairs, three commit rows (two for A, one for B) -> tips are the newest sha per chair, commits 2/1, files_touched is the sorted union; a seated chair with no commit row shows nulls, commits 0, and is still listed; harness equals the seat row's `to`; rail card carries `provenance` and the existing rail assertions in happy_path_test still pass; the rail and MCP blobs contain no 'token' and no resume argv (g2's test_public_rail_usage_is_null_never_zero_and_carries_no_token gates this); MCP tools/list includes `provenance`.
- done when: `CV provenance` and `CV rail` from the luna1 worktree show luna1's branch tip; python test/run.py GREEN; hook note with the printed tips as evidence.


### 3. `convoy rebase --check`: which siblings touched my files, read-only
provenance.rebase_check(worktree, base='feat/happy-path-proof', root=None) is READ-ONLY: it never runs git rebase/merge/fetch/checkout. Steps: base_sha = rev-parse <base> (rc!=0 -> {ok:false, error:'base ref absent'}, no exception); merge_base = merge-base <base> HEAD; behind = rev-list --count HEAD..<base>; my_files = diff --name-only <merge_base>..HEAD plus `status --porcelain` paths under key `uncommitted`. Siblings: (a) every local branch matching convoy/* except my own branch, source 'git': sibling_base = `git merge-base <branch> HEAD` PER SIBLING (do not reuse my base merge-base: convoy/g1 forked at b783950 and carries a41a91d, a re-authored copy of 6fe15c1, so a shared merge-base would turn the two-dot diff into a tree compare), files = diff --name-only <sibling_base>..<branch>, sha = rev-parse <branch>, chair = the seats.jsonl chair whose worktree has that branch checked out, else null; (b) every chair with feed kind=commit rows and no local branch, source 'feed': files = union of files from its commit rows whose sha is NOT in my history (`merge-base --is-ancestor <sha> HEAD` rc!=0, or sha unknown to local git); this gives 'git' and 'feed' the same meaning: work not yet in my HEAD. overlapping_files = intersection with my_files (committed + uncommitted). Card: {ok, base, base_sha, merge_base, behind, my_files, uncommitted, siblings:[{chair, branch, sha, source, files, overlapping_files}], note:'committed refs only; a sibling's uncommitted work is invisible to git by design', action: 'rebase' when behind>0 or any overlap else 'clean'}. Unknown stays null. CLI: `convoy rebase --check [--base <ref>] [--worktree <path>]`; without --check it refuses: 'this verb only reports; run git rebase yourself'.

- files: src/convoy/provenance.py, src/convoy/cli.py, test/demo/rebase_check_test.py
- tests: Temp repo: base branch, my branch, two sibling branches forked at different points, one edits the same file as mine, one does not -> overlapping_files names exactly that file for the first and is [] for the second, and each sibling's files list reflects its own merge-base; base advances one commit -> behind 1, action rebase; missing base ref -> ok:false, no exception; a chair known only from feed commit rows (no local branch) is reported with source 'feed' and its files exclude shas already in my history; `convoy rebase` without --check refused; a fake _git records every argv and the test asserts only read-only subcommands (rev-parse, merge-base, rev-list, diff, status, branch, for-each-ref) were invoked.
- done when: Run from C:\Users\marco\ola\convoy-wt-happy-wt-luna1 after you have touched src/convoy/cli.py: the card names convoy/g1 (and convoy/g2, which has merged g1) as source 'git' with src/convoy/cli.py in overlapping_files; the card's note says committed refs only; python test/run.py GREEN; hook note with the card as evidence.


### 4. Docs: name kind=commit in the SoT and the check in the ladder
docs/CONVOY_SOT.md: add `commit` to the open kind list and a short 'Commit row (kind=commit)' block with the exact JSON shape from slice 1; add step 0 'rebase --check' before 'launch' in the ladder. docs/HAPPY_PATH.md frame 6: one line `convoy committed --as-me` after the commit and `convoy provenance` beside rail. No other prose. Every field named in the docs must be a field the code writes; g2's grep-gate (test_sot_page_names_the_one_architecture, test_src_has_no_parallel_store) reads these pages. Rebase first: g1 and g2 may have edited CONVOY_SOT.md on their branches; if `git diff --name-only $(git merge-base convoy/g1 HEAD)..convoy/g1 -- docs/` is non-empty, keep your block additive at the end of the kinds section so the integrator's merge is mechanical.

- files: docs/CONVOY_SOT.md, docs/HAPPY_PATH.md
- tests: test/demo/provenance_read_test.py asserts the JSON example in CONVOY_SOT.md parses and its key set equals the key set of the row commit_row writes (doc and code cannot drift).
- done when: Docs committed on convoy/luna1 with the keys test GREEN; hook note posted.


**Refuter findings folded in:** STALE PROVENANCE, wrong rebase guidance: the brief's `provenance_observed` and `rebase_protocol` say convoy/g1 has UNCOMMITTED edits to cli.py, index.py, mcp_ht | Slice 3 done_when is premised on the stale fact: it tells luna1 to write into the card that 'g1's uncommitted work is not visible to git; say so as a limitation | UNVERIFIED tool path for the dogfood step: the `convoy` on PATH is C:\Users\marco\.local\bin\convoy.cmd -> C:\.grok\Invoke-Convoy.ps1, a legacy PowerShell shim  | send_body points luna1 at docs/briefs/LUNA_BRIEFS.md (section luna1); that file does not exist in C:/Users/marco/ola/convoy-wt-happy/docs/briefs (only HAPPY_PAT | Cannot verify no overlap with luna2: luna2's brief was not supplied to this check. If luna2 was given anything touching provenance rows, rebase-check, rail keys | Minor, unverified: `--as-me` on codex depends on whoami's process-ancestry match (panes.py:267 notes Windows codex panes carry thin evidence; match falls back t | provenance_observed is stale against the live tape: it says convoy/g1 tip is a41a91d with 10 UNCOMMITTED files, but `git -C convoy-wt-happy-wt-g1 log` shows tip | Slice 3 done_when depends on that stale fact: 'g1's uncommitted work is not visible to git; say so in the card as a limitation, do not fake it' would have luna1 | send_body tells luna1 to run `seated --seat luna1-happy-path --token f259aab5...`, but feed.jsonl line 22 (02:19:37Z) already has luna1's `seated` row citing th | Slice 3 source='feed' semantics are inconsistent with what the card claims: the card says siblings' files are 'since the merge-base', but the feed path computes | Slice 2 introduces `harness` on the per-chair provenance object; seats.jsonl carries the occupant harness under `to` (CONVOY_SOT.md line 50). Renaming is fine f | Slice 1 dedupe 'same chair+sha already in the feed -> ok:true, duplicate:true' — reading the feed to decide is consistent with the single-writer rule; no parall | REBASE PROTOCOL IS SELF-CONTRADICTORY / NOT EXECUTABLE: the `rebase_protocol` text says never rebase onto a sibling, 'cherry-pick nothing', THEN 'hand-merge you | `--as-me` HAS NO FALLBACK AND IS UNVERIFIED FOR CODEX ON WINDOWS: slice 1 says `committed --as-me` resolves the chair 'exactly as hook --as-me does' and 'a chai | REUSING gitstate._run CANNOT HONOR THE NULL SEMANTICS THE BRIEF DEMANDS: _run (gitstate.py:8-17) returns None both on non-zero exit AND on empty stdout (`return | PROVENANCE_OBSERVED IS STALE AND THE 'KNOWN OVERLAP' IS WRONG: as of this read, convoy/g1 tip is efd5e26 (committed 02:19Z; feed note at 02:19:35Z), which COMMI | SLICE 3 FEED-SOURCE PATH IS UNDERSPECIFIED AND ITS done_when IS PARTLY UNSATISFIABLE: 'files newer than my last commit row ts' is undefined when luna1 has no co | SEND_BODY POINTS AT A FILE THAT DOES NOT EXIST ON ANY BRANCH: 'your brief is docs/briefs/LUNA_BRIEFS.md' - only docs/briefs/HAPPY_PATH_ULTRACODE.md exists on fe | MINOR: the 'before every push' hand command uses `$(git merge-base ...)` (works in PowerShell and bash) but says nothing about which shell codex runs; `convoy` 


## luna2-happy-path: Outside-harness join + MCP attach path, tested end to end, with per-chair provenance on the rows a joiner writes

**Provenance observed at drafting:** Read 2026-09-05 ~04:20Z. Thread root C:/Users/marco/ola/convoy-wt-happy on feat/happy-path-proof at 6fe15c1; docs/briefs/ contains only HAPPY_PATH_ULTRACODE.md. g1 (C:/Users/marco/ola/convoy-wt-happy-wt-g1, convoy/g1): HEAD 999b7f9 'Purge ola_runner and move handoffs to .convoy/handoff/' (13 files: README.md, SPEC.md, skills/neuron-identity/SKILL.md, src/convoy/cli.py, context.py, harness_skills/neuron-identity/SKILL.md, identity.py, registry.py, synapse.py, test/demo/feed_note_provenance_test.py, limited_send_ask_test.py, phase1_threaded_context_test.py, seat_lifecycle_test.py) on top of efd5e26 'Index hygiene: throwaway test home, threads --prune, recent() for the picker' (10 files incl. src/convoy/index.py, cli.py, mcp_http.py, test/__init__.py, test/demo/__init__.py); both pushed to origin/convoy/g1. g1 UNCOMMITTED: M README.md, M plugin/convoy/skills/convoy/SKILL.md, M skills/convoy/SKILL.md, M src/convoy/cli.py (hunks at L13, L225, L567), ?? src/convoy/start.py, ?? test/demo/start_test.py (the /convoy --start slice). g1 feed note 04:12:57Z: slice 2 ola purge done, synapse.ola_runner deleted, handoff is .convoy/handoff/<chair>-<ts>.md. g2 (C:/Users/marco/ola/convoy-wt-happy-wt-g2, convoy/g2): HEAD 296a8bc 'Allow labelled .ola legacy fallbacks in the handoff grep-gate' over edc7045 (merge convoy/g1 @999b7f9) over 66b7379 'Failure-path tests and grep-gates pin the happy-path verify contract' (test/demo/grep_gate_test.py +214 with ArgvAndHookGate, PublicCardGate incl. no-token rail assertion, SoTWriterGate, LimitedAskHandoffPath; test/demo/happy_path_failure_test.py +380); clean tree; pushed to origin/convoy/g2. g2 notes 04:14-04:17Z: 66b7379 pushed, happy_path_test GREEN on merged g1, re-verified 999b7f9, grep_gate GREEN; remaining RED it ran: convoy start missing, crew OSError orphans chairs, whoami items. luna1/luna2 worktrees at 6fe15c1, clean; luna1 noted 02:22:45Z 'no implementation brief received'; luna2 seated 02:20Z and noted 02:21:36Z 'no implementation brief received; stopping without code changes'. No chair has posted a provenance writer. Code points: lifecycle.seated_ack(root, session_id, token) L146 writes extra={token} only, no cwd; layer.neuron_note L123-135 is the hook-note writer, extra={truncated} only; gitstate.py has _run and git_state only; harness_contract: cloud offered only when mode=='interactive-session' (claude yes; codex 'task' -> validate_where refuses; grok unverified); mcp_http.py L243 send live never steals a TUI (naming a live seat queues); graph_html.py L75 holds the only 'no-steal' string; cli.py attach positional convoy_id L181-182, bind --thread L184-185; test/run.py at 6fe15c1: 535 tests OK (skipped=1), discovery *_test.py under test/demo.

**Rebase protocol:** Refs that exist (verified 2026-09-05 ~04:20Z via git ls-remote): origin/convoy/g1 = 999b7f9, origin/convoy/g2 = 296a8bc (contains g1), origin/feat/happy-path-proof = 6fe15c1. Shared local branches convoy/g1, convoy/g2, convoy/luna1, convoy/luna2 also exist in the worktree-shared repo. Step 1, before any edit: `git fetch origin && git merge origin/convoy/g2` into convoy/luna2 (one merge brings all committed g1+g2 work; g2 itself merges g1 this way, so match the pattern), then `python test/run.py`. Before EVERY later push: `git fetch origin`; then `git log origin/convoy/g1 origin/convoy/g2 origin/feat/happy-path-proof --not HEAD -- <every file you edit>`; for each hit `git merge <that branch>` (merge, not cherry-pick, so shas stay stable). Rebase rule: `git rebase origin/feat/happy-path-proof` is allowed ONLY while convoy/luna2 has never been pushed and no chair has cited one of your shas on the feed; after your first push, integrate by merge only. Never force-push; never rewrite a sha a hook note cites. Uncommitted peer work: before editing any file, run `git -C C:\Users\marco\ola\convoy-wt-happy-wt-g1 status --short` and the same for -wt-g2 (read-only); if the file you need is modified or untracked there, do not edit it, post a hook note to that chair naming the file, and defer that edit to a later commit (right now that list is README.md, plugin/convoy/skills/convoy/SKILL.md, skills/convoy/SKILL.md, src/convoy/cli.py, src/convoy/start.py, test/demo/start_test.py on g1). Before starting and before every push run `convoy --root C:\Users\marco\ola\convoy-wt-happy feed --since 3h` and read every note from g1/g2/luna1 that names a file you touch. Each slice: fetch+merge, `python test/run.py`, commit small, push convoy/luna2, then `convoy --root C:\Users\marco\ola\convoy-wt-happy hook note "<slice>: <sha> <files> <test totals>" --as-me --to grok-bot`. Never squash.


### 0. Base yourself on the shared tips before writing a line
convoy/luna2 is at 6fe15c1 (thread base). Both grok branches are committed AND on origin: convoy/g1 = 999b7f9 (efd5e26 index hygiene + 999b7f9 ola purge), convoy/g2 = 296a8bc (merges g1 at 999b7f9, adds test/demo/grep_gate_test.py and test/demo/happy_path_failure_test.py). g1 additionally has UNCOMMITTED work in its worktree (README.md, plugin/convoy/skills/convoy/SKILL.md, skills/convoy/SKILL.md, src/convoy/cli.py hunks at ~L13/L225/L567, new src/convoy/start.py + test/demo/start_test.py) — that is the /convoy --start slice and is not reachable by any git command. First act: `git fetch origin` then `git merge origin/convoy/g2` into convoy/luna2 (g2's tip is a superset of g1's committed work, so one merge brings in the CONVOY_HOME test guard, recent(), ola purge, and both gates without a fork). Run `python test/run.py` and post the totals as your first hook note. Do NOT edit README.md, SPEC.md, plugin/, skills/, or src/convoy/cli.py in any slice: g1's uncommitted diff is on those files right now.

- files: none
- tests: python test/run.py on the merged tree; paste totals.
- done when: convoy/luna2 contains 296a8bc in its history (`git merge-base --is-ancestor 296a8bc HEAD`), test/run.py GREEN, hook note posted with the totals and the merged sha.


### 1. docs/OUTSIDE_JOIN.md: the no-cwd join walkthrough (cards quoted from slice 2, never pre-stated)
A claude/codex/grok session started OUTSIDE Convoy (cwd in no worktree, no .convoy pointer) joins a thread. Document the exact command sequence, every card quoted verbatim from outside_join_test.py's run: `convoy threads` (finder only; the picker input is g1's `recent(limit)` in src/convoy/index.py, already on your branch after slice 0; never auto-pick newest) -> pick a root -> `convoy --root <root> whoami` returns ok=false, chair=null WITH an ask -> `bind --thread <root>` and `attach <convoy_id>` (attach takes a positional convoy_id: cli.py L181-182; take it from the field the `threads`/`whoami` card exposes for that root, and if no card exposes it the test proves that gap and the doc says so; never guess an id) -> `join --to <harness> --where local` (seat row + join feed row + boot prompt) -> the neuron proves it with `seated --seat <me> --token <t>` -> conductor `send --to <chair>` (delivery queued, delivered false) -> the neuron `inbox --seat <me> --drain` and authors an ack note citing the token -> a `send --live` at that chair: quote WHATEVER the wire returns (mcp_http.py L243 says live never steals a TUI but the observed card may be queued rather than a refusal; graph_html.py L75 has the only 'no-steal' string). The doc inherits the observed card; it does not assert one. Tokens: state plainly that the token is minted into seats.jsonl and also appears in the on-disk feed join/seated rows (lifecycle.py L72-76, L153-157), and that redaction is an envelope concern: no CLI or MCP card ever carries it (CONVOY_SOT.md L53). Every unknown on every card is JSON null. No new verbs unless slice 2 proves a gap; any addition composes layer.hook extras, never a parallel store. No PTY-ownership language anywhere in this page: Convoy attaches by MCP or by a pane host it does not own; Herdr owns PTYs (SPEC.md L242).

- files: docs/OUTSIDE_JOIN.md, docs/HAPPY_PATH.md (one pointer line to the new page, nothing else)
- tests: None of its own; slice 2 is its proof (test/demo/wizard_prose_matches_wire_test.py is the prose-matches-wire pattern).
- done when: The command sequence is exactly what outside_join_test.py runs, every card is pasted from the test output, no `ola`, no invented ids/usage, and `grep -niE "convoy (owns|pastes into|dumps) .*pty|pty paste" docs/OUTSIDE_JOIN.md` returns nothing.


### 2. test/demo/outside_join_test.py: end-to-end proof on a temp CONVOY_HOME
Walk slice 1 in-process against a real git temp repo (happy_path_test.py is the pattern). Rely on the CONVOY_HOME guard already in test/__init__.py and test/demo/__init__.py (g1 efd5e26, on your branch after slice 0); do not duplicate it. Create two threads so a picker is required, run from a cwd inside NO worktree, and assert: threads lists both roots with present=true; whoami on the chosen root returns ok=false/chair=null WITH an ask, never a guess, and writes nothing (feed length unchanged); attach hooks one `attach` row and returns since=null on first attach; join mints a token that is present in seats.jsonl; the token is ABSENT from every CLI card returned by threads/whoami/attach/join/send/inbox (the on-disk feed rows may carry it; do not assert otherwise and do not strip it, that would be a SoT change); send queues (runner inbox, delivery queued, delivered false); pending() shows the row; drain + the chair's own ack note citing the token is the only receipt; a second `send --live` to the chair: capture the card and assert only the invariant that no pane was stolen (no bring_up/launch call), recording the observed delivery field in the test's docstring for slice 1 to quote.

- files: test/demo/outside_join_test.py
- tests: The file itself; `python test/run.py`; paste the pass count in the hook note.
- done when: outside_join_test.py GREEN, test/run.py GREEN (totals pasted), and the test asserts index_path() is under the temp home (nothing under the real ~/.convoy).


### 3. test/demo/mcp_attach_test.py: the MCP attach variant, no pane; cloud only where the contract offers it
Same join over the in-process MCP handler (src/convoy/mcp_http.py; phase_mcp_http_test.py / mcp_wizard_verbs_test.py are the patterns). Two chairs, because the harness contract splits them: (a) a CODEX chair joined where=local: seat row has effort='high' declared and effort_applied=false (codex has no effort flag); assert no bring_up/launch call happens on join. (b) `join` with where=cloud on CODEX must be REFUSED (harness_contract.validate_where L235-241: codex cloud mode is 'task'); assert the refusal card names the mode and writes no seat and no feed row. (c) a CLAUDE chair joined where=cloud (the only harness where cloud is offered, _cloud_offered L211-214): seat written, no pane, no worktree (convoy.py L121-133), `await_seated` reads stale until `seated` with the boot-prompt token flips it to connected; an ack citing a never-minted token reads stale, not connected. `inbox` read is public; `inbox drain=true` sits behind the write gate (mcp_http.py L817-828). Token assertion is SCOPED: regex the join, seated, await_seated and inbox cards this test produces for the minted token, zero hits. Do not write a rail-wide token regex: g2's PublicCardGate.test_public_rail_usage_is_null_never_zero_and_carries_no_token (test/demo/grep_gate_test.py, on your branch after slice 0) already owns that; cite it in a comment.

- files: test/demo/mcp_attach_test.py, src/convoy/mcp_http.py (only if a gate or a null is wrong and the test proves it; g1 efd5e26 and g2 296a8bc both touched this file, so any edit happens after slice 0 and stays a minimal hunk)
- tests: The file itself; public_wire_redaction_test.py, phase_mcp_http_test.py and grep_gate_test.py must stay GREEN.
- done when: mcp_attach_test.py GREEN with (a),(b),(c) as separate test methods; zero token hits across its cards; docs/HAPPY_PATH.md section 'What cloud means here' still matches the wire.


### 4. Provenance on the rows an outside joiner writes: seated ack + hook note, via layer/lifecycle, null over MCP
Marco wants temporal knowledge of what each chair wrote: branch + sha + files touched, as feed rows under the existing .convoy JSON architecture (CONVOY_SOT.md), never a parallel store. Synapse rows from the conductor already carry worktree/git_branch/git_sha. Extend the two rows the CHAIR authors: the `seated` row (lifecycle.seated_ack L146-159, currently extra={token}) and the `note` row (layer.neuron_note L123-135, currently extra={truncated}; this is the writer, not cli.py). Do NOT touch the join row: it is authored by the conductor and chair provenance on it would be misattributed. Mechanism: add an optional `provenance: dict | None` argument to seated_ack and neuron_note, merged into extra when given. The CLI path supplies git provenance from the caller's cwd; the MCP path supplies None, so seated/note over MCP or on a where=cloud chair carry worktree/git_branch/git_sha/files = null, never the MCP host's git state. BECAUSE cli.py is under g1's uncommitted edit right now, do the library + tests first and land the two CLI call-site edits (hook note --as-me ~L252-256, seated) as a SEPARATE final commit only after `git log origin/convoy/g1 -- src/convoy/cli.py` shows g1's --start commit landed (or g1 notes it on the feed); if it has not landed when you are otherwise done, post the blocker as a hook note and stop there. Add to src/convoy/gitstate.py (currently only _run and git_state) a `files_touched(cwd)` helper: `git diff --name-only HEAD` (working tree vs HEAD) unioned with `git diff --name-only @{upstream}...HEAD` when an upstream is configured; record `files_base` as '@{upstream}' or 'HEAD' accordingly so a reader knows the base; both null when not a git worktree or git is absent. Never hardcode feat/happy-path-proof or any branch name in library code (gitstate.py header: never invent main); no base branch is recorded anywhere under .convoy, so an invented merge-base would violate the honesty bar. Amend docs/CONVOY_SOT.md L43 (synapse provenance list) to name the seated/note provenance fields and files/files_base in the same commit. Tests go in a NEW file: g1 999b7f9 edited test/demo/feed_note_provenance_test.py (ola_runner import removed) and that file stays g1's; you only extend by adding your own module.

- files: src/convoy/lifecycle.py (seated_ack provenance param), src/convoy/layer.py (neuron_note provenance param), src/convoy/gitstate.py (files_touched helper), docs/CONVOY_SOT.md (L43 field list amendment only), test/demo/outside_join_provenance_test.py (new), src/convoy/cli.py (two call sites, separate final commit, only after g1's --start lands)
- tests: outside_join_provenance_test.py: note with provenance from a temp git worktree carries branch/sha/files/files_base; note from a non-git cwd carries nulls; seated row via CLI path from a worktree carries branch/sha; seated and note via the MCP handler carry nulls; no new kind, no new file under .convoy/; feed_note_provenance_test.py and grep_gate_test.py stay GREEN.
- done when: Your own hook notes on the live tape show git_branch=convoy/luna2, a real sha, and the files you touched, readable via `convoy feed --since`; g2's grep_gate_test.py stays GREEN with no second envelope; the CLI wiring commit exists or a blocker note names g1's uncommitted cli.py as the reason it does not.


### 5. Landscape honesty audit: report, do not rewrite, and no second grep-gate
g2 owns grep-gates (constraint 1) and g1 has README.md, plugin/ and skills/ SKILL.md uncommitted, so this slice writes NO prose outside docs/OUTSIDE_JOIN.md and docs/HAPPY_PATH.md and adds NO gate file. Read README.md, SPEC.md (Herdr row L242 already states Herdr owns PTYs and Convoy does not use PTY paste; PTY denials at L292, 442, 480, 1007), docs/HAPPY_PATH.md, and both SKILL.md files. For every sentence that could still be read as Convoy owning, pasting into, or dumping a PTY, post ONE hook note to g2-happy-path listing file:line, the quoted sentence, and the proposed rewrite, so g2 can add the phrasing to its existing gate and g1 can fix its files when it commits. Fix only docs/OUTSIDE_JOIN.md and docs/HAPPY_PATH.md yourself (slice 1 already keeps them clean). If the audit finds nothing, the note says so with the grep you ran.

- files: docs/OUTSIDE_JOIN.md, docs/HAPPY_PATH.md
- tests: None new; test/run.py stays GREEN.
- done when: One hook note to g2-happy-path (cc grok-bot) with the file:line list or the empty-result grep, and zero PTY-ownership phrasing in the two pages you own.


**Refuter findings folded in:** Slice 4 collides with g1's UNCOMMITTED ola purge on the same test file. g1 worktree diff (C:/Users/marco/ola/convoy-wt-happy-wt-g1, `git diff`) deletes `ola_run | provenance_observed is stale: it says g1's index hygiene is 'UNCOMMITTED ... as of 02:18Z'. It is committed: convoy/g1 HEAD is efd5e26 'Index hygiene: throwaway | Slice 3 is internally inconsistent with the harness contract. It asks for `join` with where=cloud writing a chair AND asserts effort_applied=false 'on a codex c | Slice 5's 'grep-gate test' overlaps g2's declared slice ('grep-gate'). g2 already has an untracked test/demo/grep_gate_test.py (C:/Users/marco/ola/convoy-wt-hap | Slice 3 partially duplicates g2's PublicCardGate.test_public_rail_usage_is_null_never_zero_and_carries_no_token (asserts no token in serialized rail after join+ | Slice 4 touches src/convoy/cli.py (hook note --as-me extras, around L250-262). g1's uncommitted diff also edits cli.py (L151 --handoff help). Different hunks, s | send_body cites 'brief docs/briefs/LUNA_BRIEFS.md' but docs/briefs/ in the thread root contains only HAPPY_PATH_ULTRACODE.md; the luna will read a pointer to a  | Slice 2 asserts "join mints a token that lands in seats.jsonl only" and slice 1 says "join row ... minted inbox token" with "no token" on the wire. The code wri | Slice 4 computes `files` as `git diff --name-only` against "the merge-base with feat/happy-path-proof" inside src/convoy/gitstate.py. That hardcodes a thread-sp | Slice 4 adds worktree/git_branch/git_sha/files to the `seated` row, and slice 3 runs `seated` over MCP for a where=cloud chair. seated_ack(root, session_id, tok | Slice 4 names the writer as src/convoy/cli.py + lifecycle.py, but `hook note --as-me` rows are produced by layer.neuron_note (layer.py:124-136), which builds ex | Slice 4 also says the outside joiner's `join` row should carry provenance (send_body: "join/seated/ack rows carry worktree, git_branch, git_sha, files") while t | REBASE PROTOCOL NOT EXECUTABLE AS WRITTEN: `git fetch origin convoy/g1 convoy/g2` fails — origin has no convoy/g2 (git ls-remote --heads origin shows only refs/ | REBASE-BEFORE-EVERY-PUSH CONTRADICTS NEVER-FORCE-PUSH: rebasing convoy/luna2 onto origin/feat/happy-path-proof after a first push rewrites shas, so the next pus | PROVENANCE_OBSERVED IS STALE, AND THE STALENESS CHANGES THE PLAN: g1's index hygiene is COMMITTED (efd5e26 'Index hygiene: throwaway test home, threads --prune, | SLICE 3 IS UNTESTABLE ON A CODEX CHAIR AS SPECIFIED: `join where=cloud` is refused for codex by harness_contract (where_options('codex')['cloud'] = offered Fals | SLICE 4 ASSUMES HELPERS THAT DO NOT EXIST AND HARDCODES A BRANCH: src/convoy/gitstate.py has only `_run` and `git_state(cwd)` (lines 8, 18) — no files/merge-bas | SLICE 1/2 ASSERT A CARD NOT VERIFIED ON THE WIRE: 'a `send --live` at that chair returns the no-steal refusal card' — src/convoy/mcp_http.py:243 says live=true  | SLICE 1 CLI SEQUENCE DETAILS UNVERIFIED: `attach` takes a positional convoy_id (cli.py:181-182), not a bare thread root; `bind --thread` is separate (cli.py:184
