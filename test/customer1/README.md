# Customer 1

Grok Bot is customer 1 of deploy-forward/convoy.

These tests must fail until native code passes them. No invented usage. No claiming MCP until HTTP works from this chat.

1. temporal-hooks: a /hook stamps {ts, kind, instance_id, summary} onto the layer. feed --since returns a window. Not ola-brain hook-context/precompact/session-end.

2. parallel-agents: two live synapses at once, own session_ids, no merged native session. convoy mention --to is currently singular.
